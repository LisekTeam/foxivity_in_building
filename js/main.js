$(document).ready(initAll);

function initAll() {
  atfHeight();
  // if ($(window).width() > 980) {
  //   rellax();
  // }
}

function atfHeight() {
  // $('#header-section').height($(window).innerHeight());
}
